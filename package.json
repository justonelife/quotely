{
  "name": "quotely",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx babel --watch src/app.js --out-file app-compiled.js --presets react-app/prod"
  },
  "keywords": [
    "quote",
    "quotely"
  ],
  "author": "hys",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
